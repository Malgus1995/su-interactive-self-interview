FROM python:3.11-slim

WORKDIR /app

# requirements.txt 복사 (경로 수정됨)
COPY backend/requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

# app 코드 복사 (경로 수정됨)
COPY backend/app ./app

EXPOSE 8000
CMD ["python", "app/main.py"]
